# Local Music Engine 구현 계획

작성일: 2026-09-22  
상태: 계획만 작성. 구현·설치·모델 다운로드·음악 생성·Git 초기화는 하지 않았다.  
구현 담당: 사용자가 별도로 작업을 맡길 GPT-Sol

목표 위치는 `/Users/jaehoseo/Desktop/vswrk/javis/local-music-engine`이다.
현재 작업에서는 해당 경로 생성과 프로젝트 외부 파일 쓰기가 차단되어,
`/Users/jaehoseo/Desktop/vswrk/javis/local-tts-engine/local-music-engine`에 계획서를 임시 보관했다.
실제 구현 시 독립 프로젝트 위치를 작업 공간으로 열고 이 문서를 옮긴다.
이 문서의 계획은 사용자가 후속 구현을 요청했을 때 수행한다.

## 1. 목표와 첫 완료 기준

Apple Silicon Mac에서 한국어 가사와 음악 지시를 받아 곡 후보를 생성하고,
사람이 비교해 고른 후보의 문제 구간을 수정한 뒤 WAV로 내보내는 개인 제작 엔진을 만든다.
입력·모델 판본·후보·검사·편집 이력을 남겨 프로젝트를 다시 열고 작업을 이어갈 수 있어야 한다.

첫 마일스톤은 다음 흐름을 실제 한국어 곡 한 곡으로 끝내는 것이다.

> 프로젝트 생성 → 전체 곡 후보 생성 → 비교 청취 → 후보 선택 → 한 구간 재생성
> → 연결부 확인 → WAV 내보내기 → 앱 재실행 후 프로젝트 복원

결과물의 품질을 우선한다. 기능 범위를 단계적으로 구현하되 품질에 필요한 검사·재시도를
자원 절약 때문에 생략하지 않는다. 음악성·한국어 가창 품질은 실제 청취로 판단한다.
자동 점수나 모델 소개의 벤치마크로 Suno와의 동등성을 주장하지 않는다.

## 2. 제품 범위

### 첫 버전에 포함

- 가사, 스타일 지시, 목표 길이, 선택적 곡 구조를 저장하는 프로젝트.
- ACE-Step 어댑터를 통한 전체 곡 생성과 여러 seed 후보 관리.
- 같은 위치를 오가며 들을 수 있는 후보 비교와 후보별 평가 메모.
- 실제 오디오 길이·파일 무결성·피크·무음 등 기본 검사와 근거 표시.
- 보컬 분리와 한국어 ASR을 이용한 가사 의심 구간 표시.
- 사용자가 선택한 구간의 재생성, 원본/수정본 비교, 선택 취소.
- 원본을 보존하는 WAV 내보내기와 모델·입력·검수 이력 기록.
- 중지, 실패 기록, 재시도, 명시적인 이어하기, 프로젝트 재열기.

### 후속 확장

- 여러 stem의 믹서, 자동화 곡선, 효과 체인, 목적별 마스터링.
- 사람의 선호 기록으로 검증한 자동 후보 순위 평가.
- 한국어 발음·스타일 LoRA, SVS 보컬 교체, 악보 편집.
- 다른 생성 모델, 선택적인 외부 GPU 작업자.

첫 버전은 범용 DAW 전체 기능, foundation model 학습, 여러 생성 모델 동시 도입,
자동 가창자 복제, 상용 서비스와의 보편적인 품질 동등성을 범위에 넣지 않는다.

## 3. 저장소 경계와 기존 TTS 자산 활용

독립 저장소와 독립 Python 환경을 사용한다. 기존 TTS 저장소를 통째로 복사하거나
TTS의 가상환경·제작 모델 기본값·개인 음성 정본을 공유 상태로 수정하지 않는다.

| 가져올 설계 경험 | 음악에 맞게 새로 설계할 부분 |
| --- | --- |
| 프로세스 수명·중지·진행 이벤트 | 곡과 구간, 음악 생성 작업 |
| 후보와 선택 결과의 분리 | 전체 곡·구간 후보의 비교와 채택 |
| 입력 고정·해시·결과 manifest | 모델 판본·seed·생성 조건·편집 계보 |
| 자동 검사와 사람 확인의 구분 | 가사 이해도·음악성·연결부 청취 |
| 실제 파일을 확인하는 이어하기 | 완료 후보 재사용과 중단 작업 복구 |

공통 라이브러리는 양쪽에서 실제로 같은 코드가 필요해진 뒤 추출한다.
첫 구현에서는 TTS 코드에 대한 런타임 의존성을 만들지 않는다.
기존 TTS의 “앱 새 제작은 캐시 우회” 정책도 음악 앱에 무조건 복제하지 않는다.
음악에서는 새 후보 생성과 기존 결과 이어하기를 명시적으로 구분한다.

## 4. 기술 선택과 아직 검증할 가정

### 초기 선택

- 생성기: ACE-Step 1.5의 공식 macOS MLX 실행 경로를 첫 검증 대상으로 삼는다.
- Python: 공식 안내의 3.11–3.12 범위에서 3.12를 출발 후보로 삼고 실제 호환성을 확인한다.
- 초기 연동: 공식 REST API를 감싼 어댑터. 상위 앱은 모델 내부 구현을 직접 호출하지 않는다.
- 작업 실행: Python 작업자, 타입이 있는 요청/결과 스키마, 파일 기반 artifact,
  로컬 작업 상태 저장. 프로젝트·작업 인덱스는 SQLite를 후보로 둔다.
- 앱: Electron + TypeScript. 생성 검증이 끝난 뒤 후보 비교 UI부터 구현한다.
- 서버: 앱 전용 FastAPI 계층은 UI 연결 시 필요에 맞게 추가한다.
  앱 서버와 ACE 서버의 프로세스·포트·상태 소유권을 구분한다.

### 확정하지 않을 항목

- 2B Turbo를 빠른 탐색 후보, XL-SFT를 품질 비교 후보로 평가한다.
  실제 한국어 곡의 청취·실패율·메모리 결과로 제작 기본값을 정한다.
- Turbo에서 고른 곡을 XL로 바꾸면 동일한 곡의 고품질 버전이 나온다고 가정하지 않는다.
  모델 변경은 새로운 후보 생성이며, 곡 보존 편집은 별도 검증 대상이다.
- 같은 seed도 백엔드·버전·연산 환경에 따라 동일한 오디오를 보장하지 않는다.
  재현 조건과 실제 출력 해시를 모두 기록한다.
- 조사 자료의 M4 Max 36GB를 예상 환경으로 두되, 실제 장비·메모리를 먼저 확인한다.
  일반 GPU VRAM 권장치를 Mac 통합 메모리 여유량으로 그대로 해석하지 않는다.
- 모델별 기능이 다르다. 공식 표에서 SFT/Turbo와 Base의 Extract/Lego/Complete 지원이
  구분되어 있으므로, 생성·구간 재생성·분리·악기 추가를 같은 기능으로 취급하지 않는다.
- MLX 지원 안내가 모든 모델과 모든 편집 기능의 Mac 동작을 보증한다고 가정하지 않는다.
  필요한 기능은 각각 실제 실행으로 확인한다.

### 모델과 라이선스 확인

구현 시 `docs/DECISIONS.md`에 채택한 코드와 가중치 각각의 URL·판본·라이선스·확인일을 남긴다.
현재 확인한 공식 정보는 아래와 같으며 실제 다운로드 대상의 조건을 다시 확인해야 한다.

| 대상 | 2026-09-22 확인 내용 | 초기 방침 |
| --- | --- | --- |
| ACE-Step 1.5 | 공식 저장소 MIT 표시, macOS MLX 런처·REST API 안내 | 첫 실측 대상. 선택 가중치 조건도 확인 |
| YuE2-3B | 공식 모델 카드 CC BY-NC 4.0 표시 | 첫 제품 경로의 의존성으로 넣지 않음 |
| SongEval | README는 CC BY-NC-SA 4.0, LICENSE는 Apache 2.0으로 표기가 다름 | 코드·가중치 적용 범위 확인 전 필수 평가기로 채택하지 않음 |
| 분리기·ASR·분석·마스터링 | 조사에서 제안됐으나 이 계획에서 개별 채택 검증을 마치지 않음 | 필요한 순서대로 런타임과 각 가중치의 조건 확인 |

Essentia·Matchering 등도 코드 및 배포 조건을 확인한 뒤 결정한다.
저장소의 라이선스와 가중치·데이터셋·출력물 조건을 혼동하지 않는다.
사용자가 제공한 연구 요약의 수치·평가·라이선스 주장은 확인된 사실과 분리해 기록한다.
공식 출처는 12절에 모았다.

## 5. 책임과 데이터 계약

```text
Electron Renderer: 프로젝트 편집, 후보 비교, 구간 선택, 파형과 검사 표시
         ↓ 제한된 IPC
Electron Main: 창·파일 접근, 앱 런타임 감독, 진행·중지 전달
         ↓ 타입이 있는 로컬 요청
Python Runtime: 프로젝트/작업 상태, 결과 파일, 검사, 편집 이력
         ↓ GeneratorAdapter
ACE-Step Worker/API: 모델 추론과 모델 메모리 수명
```

Python 런타임을 프로젝트와 작업 상태의 정본으로 둔다. UI는 정본을 조회하고 명령을 보낸다.
초기 CLI에서도 같은 작업 함수를 호출한다. UI를 위해 생성·검사 로직을 다시 구현하지 않는다.
모델 추론 환경과 분석 환경의 의존성이 충돌하면 작업자를 별도 환경으로 분리한다.
로컬 API는 loopback에만 바인딩하고 앱이 소유한 런타임을 인증한다.
renderer에 임의의 파일 접근·명령 실행 API를 노출하지 않는다.

| 기록 | 최소 포함 내용 |
| --- | --- |
| Project | schemaVersion, projectId, 가사 원문, 스타일 지시, 목표 길이·구조, 선택 후보 |
| GenerationRequest | 고정 입력, 모델·백엔드 판본, 실제 seed, 전체 파라미터, 입력 artifact 참조 |
| Artifact | artifactId, 경로, 파일 해시, 실제 샘플 수·sample rate·채널, 생성 작업 |
| Candidate | 생성 요청, 출력 artifact, 검사 참조, 사람 평가, 부모 후보·편집 범위 |
| Finding | 검사 종류, 실제 오디오 구간, 관측값·근거, 확신도, 경고/실패 구분 |
| Job | 상태, 단계, 입력·출력 참조, 진행, 오류, 취소 요청, 시작·종료 시각 |
| Revision | 바뀐 입력·편집 결정, 이전 판본, 선택 결과, 내보내기 참조 |

가사 원문과 모델 입력용 정규화 가사는 별도로 저장한다. 한국어 발음문 치환이나
가사 수정은 원문을 덮지 않으며, 어떤 입력으로 만든 후보인지 명확히 남긴다.
곡의 실제 시간은 파일의 샘플 수에서 얻는다. 요청 길이나 ASR 시각을 실제 길이로 쓰지 않는다.
비교·편집 시간은 공통 시간 단위로 저장하고, 파일마다 sample rate에 맞춰 샘플로 변환한다.

원본 오디오·과거 후보는 불변으로 유지한다. 수정은 새 artifact와 revision으로 남긴다.
파일 기록은 임시 파일 → 닫기·검증 → 원자적 반영 순서로 수행한다.
진행 상태와 실제 파일의 일치를 확인하며, 파일 없는 성공 상태를 허용하지 않는다.

## 6. 작업 흐름·캐시·중지

```text
입력 고정 → 후보 생성 → 파일/음질 검사 → 비교 청취 → 후보 선택
                       ↓
                 보컬 분리 → 가사 분석 → 의심 구간 표시
                                           ↓
선택 후보 + 구간 편집 요청 → 재생성 후보 → 구간/연결부 재검 → 채택 또는 원본 유지
                                                              ↓
                                                    믹스/출력 → 내보내기
```

- 작업 노드는 입력 artifact 해시·입력 내용·모델 판본·seed·파라미터·알고리즘 버전을 기록한다.
  같은 기록은 추론의 수학적 결정성을 보증하는 뜻이 아니라 재사용 가능한 결과의 식별자다.
- 새 후보 생성은 새로운 작업으로 실행한다. 기존 후보를 반환하는 동작은 이어하기로 표시한다.
- 이어하기는 파일 존재·해시·필수 메타데이터가 맞는 완료 노드만 재사용한다.
  중단·실패 노드는 성공으로 간주하지 않는다.
- 후보 하나의 실패는 기록하고 나머지 후보를 계속한다. 공통 입력·모델 오류나 사용자 중지는
  전체 실행에 전파한다. 부분 완료와 전체 완료는 별도 상태다.
- 중지는 앱이 소유한 작업자와 하위 프로세스에 전달한다. 관련 없는 TTS나 사용자 프로세스를
  종료하지 않는다. 불완전한 출력은 완료 후보와 분리한다.
- gain·pan 등 믹스 변경은 추론 결과를 무효화하지 않는다.
- 가사 한 줄 수정 시 전체 곡 맥락이 필요한 생성과 국소 편집을 구분한다.
  해당 줄만 다시 생성해도 된다는 가정을 작업 그래프에 고정하지 않는다.
- 대형 생성기·분리기·ASR은 초기에는 순차 실행하고 필요 시 프로세스를 종료해 메모리를 회수한다.
  메모리 최적화 전후 품질이 달라지는 선택은 측정과 청취 근거를 남긴다.

## 7. 품질 판정과 부분 재생성

### 객관적인 실패와 청취할 경고를 구분

파일 손상, 디코딩 실패, NaN, 비어 있는 오디오처럼 명확한 문제는 실패로 처리한다.
피크·무음·실제 길이·loudness는 관측값과 판정 기준을 함께 남긴다.
의도된 쉼·페이드·다이내믹을 자동 결함으로 단정하지 않는다.
음량 보정으로 원본의 찌그러짐이 복구됐다고 판단하지 않는다.

한국어 ASR·자모 비교는 처음에는 의심 구간을 찾는 보조 검사다.
가창·겹보컬·분리 손실에 따른 오탐을 사람의 판정과 대조한 뒤 자동 재시도 범위를 정한다.
TTS에서 쓰던 임계값을 그대로 복제하지 않는다. 같은 ASR의 반복 실행을 독립 판독 여러 개로
계산하지 않으며, 강제 정렬 결과만으로 가사를 정확히 불렀다고 판정하지 않는다.

BPM의 절반/두 배 템포 해석, 상대조·전조, 구조 라벨 추정은 확신도와 함께 표시한다.
검증되지 않은 구조·BPM·key 추정만으로 곡을 자동 폐기하지 않는다.
기술 검사, 가사 이해도, 음악적 선호, 사람의 최종 선택을 하나의 점수로 뭉치지 않는다.

### 재생성 범위와 채택

- 사용자가 고른 범위와 모델에 실제 제공한 문맥 범위를 별도로 기록한다.
- 첫 단위는 안정적으로 편집 가능한 음악 구간이다. 한 줄 단위 편집의 정확성을 약속하지 않는다.
- 모델이 전체 길이나 타이밍을 바꾸었는지 확인한 뒤 대체 가능한 구간을 결정한다.
- 수정 구간 안의 가사뿐 아니라 시작·끝의 박자, 음색, 반주, 음량과 연결감을 검사한다.
- 크로스페이드만으로 음악적 연결이 해결됐다고 간주하지 않는다.
- 원본/수정본의 비교 구간에 앞뒤 문맥을 포함하고, 비교 재생 음량 보정은 원본 파일에 굽지 않는다.
- 마음에 드는 수정이 없으면 원본을 유지한다. 재시도 예산 소진을 검수 통과로 표시하지 않는다.

초기 후보 수 4개는 실험의 출발값이다. 후보 수·재시도 수는 설정과 작업 기록에 남기고,
특정 횟수를 품질 보증이나 모든 작업의 고정 상한으로 취급하지 않는다.

## 8. 구현 순서와 단계별 완료 조건

### P0 — 환경과 실제 생성 가능성 확인

1. 목표 디렉토리를 작업 공간으로 열고 새 Git 저장소·최소 문서·ignore 규칙을 만든다.
2. 장비·OS·Python·사용 가능 메모리·디스크를 확인한다.
3. 사용할 공식 코드와 가중치 판본·라이선스·다운로드 용량을 확인한다.
   설치·다운로드 전에 목적과 예상 용량을 사용자에게 알린다.
4. 독립 환경에서 ACE-Step 공식 실행 경로로 한국어 짧은 샘플과 전체 곡을 생성한다.
5. 동일 입력의 여러 seed 후보와 필요한 경우 XL 비교 후보를 만든다.
   출력·설정·로그·실측 시간·피크 메모리·swap 관측을 기록한다.
6. 생성뿐 아니라 Mac에서 필요한 repaint 경로가 작동하는지 작은 실험으로 확인한다.

완료 조건: 실제 재생 가능한 결과, 조건을 재실행할 명령, 메모리/실패 기록,
대표적인 품질 문제와 필요한 다음 실험이 문서에 남아 있다.
문서의 지원 표기나 import 성공만으로 이 단계를 통과시키지 않는다.
기본 생성 품질이 목표에 미치지 못하면 UI 확장보다 프롬프트·모델 비교를 먼저 진행한다.

### P1 — CLI에서 프로젝트와 후보 관리 완성

1. Project·Job·Artifact·Candidate 스키마와 버전을 정의한다.
2. ACE 어댑터로 생성, 상태 조회, 중지, 결과 수집을 통일한다.
3. 후보 순차 생성, 부분 실패, 결과 해시, 기본 QC를 연결한다.
4. 후보 선택·WAV export·프로젝트 재열기를 CLI로 완성한다.
5. 중단 후 명시적인 이어하기에서 완료 후보 검증과 실패 후보 재실행을 확인한다.

완료 조건: 앱 없이 한 프로젝트의 후보 생성 → 선택 → 내보내기 → 복원이 가능하다.
실패와 취소를 성공으로 표시하지 않고, 원본 후보가 보존된다.

### P2 — 후보 비교 앱과 가사 의심 구간

1. 프로젝트 입력·작업 진행·후보 비교 화면을 만든다.
2. 파형, 실제 길이, 동일 위치 비교, 후보 선택과 청취 메모를 연결한다.
3. 라이선스와 Mac 동작을 확인한 분리기·ASR을 작업자로 연결한다.
4. 가사 의심 구간을 클릭해 듣고, 실제 문제인지 사람의 판정을 남길 수 있게 한다.
5. 앱 종료·재실행 시 프로젝트와 선택·작업 상태를 복원한다.

완료 조건: 사용자가 후보를 고르고 가사 경고를 확인할 수 있다.
가사 검사의 오탐·미탐 사례를 남기며 “경고 없음”과 “사람 확인 완료”를 구별한다.

### P3 — 구간 편집과 첫 마일스톤 완주

1. 실제 시간 범위와 수정 지시를 받아 repaint 후보를 만든다.
2. 모델별 편집 제약과 실제 출력 범위를 처리한다.
3. 수정 구간 및 앞뒤 연결 비교, 채택·되돌리기, revision 기록을 구현한다.
4. 선택된 결과를 원본과 구별되는 WAV로 내보낸다.
5. 전체 곡 한 곡을 편집까지 끝내고, 저장 후 다시 열어 결과를 확인한다.

완료 조건: 1절의 첫 마일스톤을 실제 모델·실제 파일·실제 앱으로 수행했다.
자동 검사 결과와 사용자 청취 상태, 남은 품질 문제를 분리해 보고한다.

### P4 — 측정에 근거한 확장

P3 결과에서 가장 큰 품질 문제를 골라 stem 믹싱, 목적별 마스터링, 자동 순위 평가,
한국어 evaluator 또는 LoRA를 추가한다. 연구 요약의 모든 도구를 일괄 설치하지 않는다.
자동 순위 평가는 사람의 선택과 얼마나 일치하는지 확인한 뒤 제작 흐름에 넣는다.

## 9. 검증 전략

제품 코드에는 실제 동작과 실패 복구를 검증하는 검사를 둔다.
테스트를 위해 서비스 구현을 복제하거나 문자열 존재 여부만 검사하지 않는다.

- 단위 검사: artifact 참조·시간 변환·선택/되돌리기·작업 상태 전이·캐시 무효화 범위.
- 통합 검사: 작업자 취소, 개별 후보 실패, 손상 파일, 중단 후 복원, 잘못된 모델 설정.
- 실제 추론 검사: 한국어 후보 생성, 구간 편집, 모델 종료 후 다음 분석 작업 실행.
- 앱 검사: 생성 → 비교 → 선택 → 구간 수정 → 내보내기 → 재열기.
- 청취 기록: 발음·누락·반복, 보컬 자연스러움, 후렴과 곡 구조, 음질, 편집 연결부.

비교용 입력은 자작/사용 권한이 있는 가사로 고정한다. 짧은 구간과 2–3분 전체 곡을 포함해
장곡에서만 생기는 구조·반복 문제를 확인한다. 한국어 중심 가사와 영어가 섞인 조건을 구분한다.
모든 조합의 무제한 생성 대신 이번에 평가할 조건·후보 수·예상 작업량을 먼저 명시한다.
작은 표본으로 전체 한국어 품질이나 Suno 대비 승률을 주장하지 않는다.

품질을 판단할 수 없는 mock 결과를 실제 모델 검증으로 보고하지 않는다.
실측하지 않은 생성 속도·메모리·실패율은 목표 또는 미확인으로 표시한다.

## 10. 문서와 파일 운영

후속 구현에서 필요한 시점에 다음 문서를 만든다. 처음부터 빈 문서를 늘리지 않는다.

| 문서 | 소유할 내용 |
| --- | --- |
| README.md | 설치·실행·사용법 |
| AGENTS.md | 저장소 작업 원칙·검사 명령·데이터 경계 |
| docs/PLAN.md | 이 계획과 범위 변경 |
| docs/ARCHITECTURE.md | 책임·스키마·작업 및 파일 계약 |
| docs/QUALITY.md | 검사 기준·청취 절차·검증의 한계 |
| docs/DECISIONS.md | 채택/보류 근거·모델 판본·라이선스 출처 |
| docs/HANDOFF.md | 현재 상태·실제 실행 명령·결과 위치·다음 작업 |

모델 가중치·개인 음성·참조 음악·생성 오디오·캐시·로그의 민감 정보·환경 비밀값은 Git에
넣지 않는다. 작은 테스트 fixture도 사용 권한과 크기를 확인한다.
프로젝트 원본과 사용자 채택 결과를 자동 정리 대상으로 삼지 않는다.

## 11. GPT-Sol에 전달할 작업문

아래 문장은 사용자가 새 프로젝트에서 구현을 시작할 때 사용할 수 있다.

> PLAN.md를 읽고 local-music-engine 구현을 시작해줘. 먼저 P0에서 Apple Silicon의
> 실제 생성·구간 재생성 가능성과 한국어 결과 품질을 확인하고, 그 근거로 P1~P3를
> 순서대로 완성해줘. 기존 local-tts-engine의 데이터와 실행 환경은 보존해줘.
> 설치와 모델 다운로드 전에는 목적과 예상 용량을 알려줘. 실제 추론·검사·청취 상태를
> 구분해서 기록하고, 검증되지 않은 자동 점수로 품질을 보증하지 마.
> 첫 목표는 한국어 한 곡을 후보 비교·구간 수정·WAV 내보내기까지 끝내고,
> 프로젝트를 다시 열어 작업을 이어갈 수 있게 하는 거야.

이 계획서를 작성한 작업에서는 GPT-Sol 작업을 생성하거나 실행하지 않았다.

## 12. 확인한 공식 자료

확인일: 2026-09-22. 구현 시작 시 선택할 정확한 판본의 조건을 다시 확인한다.

- [ACE-Step 1.5: 실행 환경·macOS MLX·REST API·모델별 기능·라이선스](https://github.com/ace-step/ACE-Step-1.5)
- [YuE2-3B 공식 모델 카드](https://huggingface.co/m-a-p/YuE2-3B)
- [SongEval README의 라이선스 안내](https://github.com/ASLP-lab/SongEval#-license)
- [SongEval LICENSE 파일](https://github.com/ASLP-lab/SongEval/blob/main/LICENSE)

기존 TTS의 설계 참조는 `/Users/jaehoseo/Desktop/vswrk/javis/local-tts-engine/docs/ARCHITECTURE.md`
및 같은 폴더의 `QUALITY.md`다. 이 문서들은 재사용할 설계 경험의 근거이며 음악 엔진의
구현 계약을 자동으로 대신하지 않는다.
